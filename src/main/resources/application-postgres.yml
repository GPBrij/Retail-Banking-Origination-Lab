spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/origination}
    username: ${DB_USERNAME:origination}
    password: ${DB_PASSWORD:change-me}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
# Portfolio profile only. Supply local environment variables and a managed schema before use.